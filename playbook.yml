- name: Github play
  hosts: ionos
  gather_facts: true
  
  vars_prompt:

    - name: parent_directory
      prompt: Name of the parent directory
      default: kubeflow-mlops
      private: false
  
  roles:
    - remote

- name: Docker check
  hosts: localhost
  gather_facts: true
  
  roles:
    - local